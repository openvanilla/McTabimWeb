/** Singleton manager for loading and querying CIN tables, symbols, and emoji metadata. */
export { InputTableManager } from './data/InputTableManager';

/** Main input method controller that drives key handling and input state transitions. */
export { InputController } from './input_method';
